import { Typography, Container } from "@mui/material";

function Dashboard() {
  return (
    <Container sx={{ mt: 5 }}>
      <Typography variant="h4">
        Dashboard
      </Typography>
    </Container>
  );
}

export default Dashboard;